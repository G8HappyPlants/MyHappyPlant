name: CI

on:
  pull_request:
    branches: [ "dev", "main" ]
  push:
    branches: [ "dev", "main" ]

jobs:
  backend:
    name: Backend (Maven build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Run verify (test & attempt package)
        run: mvn -B -f backend/app/pom.xml clean verify
        env:
          DB_AES_KEY: ${{ secrets.TEST_DB_AES_KEY }}
          JWT_SECRET: ${{ secrets.TEST_JWT_SECRET }}
          DB_SHA256_HMAC_KEY: ${{ secrets.TEST_SHA256_HMAC_KEY }}

  frontend:
    name: Frontend (Node build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build